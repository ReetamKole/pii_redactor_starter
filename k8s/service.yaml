apiVersion: v1
kind: Service
metadata:
  name: pii-redactor-service
  namespace: pii-redactor
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  
  type: NodePort
  selector:
    app: pii-redactor
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8081  
    name: http 